# Required: your Anthropic API key. Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=your-key-here

# Service-to-service credential this app uses to call the downstream
# (mock) order-management service. Fine to leave as the default for local
# demo purposes.
ORDER_SERVICE_PASSWORD=service-secret
